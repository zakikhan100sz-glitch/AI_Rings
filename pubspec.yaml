name: airings_mobile
description: AIRings — AI-powered early detection platform for glucose instability.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  google_fonts: ^6.2.1
  fl_chart: ^0.70.2
  connectivity_plus: ^6.1.4
  sqflite: ^2.4.2
  intl: ^0.20.2
  google_sign_in: ^6.1.0
  sign_in_with_apple: ^8.1.0
  shared_preferences: ^2.3.0
  path: ^1.9.0
  http: ^1.2.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
